import { createTheme } from '@mui/material/styles';

const themeElder = createTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#C29572',
      },
      secondary: {
        main: '#F7EEE7',
      },
    },
});

export default themeElder