import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      type: 'light',
      primary: {
        main: '#7E9459',
      },
      secondary: {
        main: '#E6EBDF',
      },
    },
});

export default theme